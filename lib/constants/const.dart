import 'package:flutter/material.dart';

const backgroundColor = Color(0XFFFFFFFF);
const textColor = Color(0XFF231f20);
const primaryColor = Color(0XFFa07178);
const pagePadding = EdgeInsets.all(20);
const borderColor = Color(0xFFe4e4e4);
const loadingColor = Color(0xFF4ea5d9);
const kGreenColor = Color(0xFF6AC259);
const kRedColor = Color(0xFFE92E30);
const kGrayColor = Color(0xFFC1C1C1);

Widget addHeightSpace(height) => SizedBox(
      height: height,
    );

Widget addWidthSpace(width) => SizedBox(
      width: width,
    );

const double kDefaultPadding = 20.0;
